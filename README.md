# fcc-periodic-table-database
Ein Terminal-Tool zur Abfrage chemischer Elemente. Ein Bash-Skript verarbeitet Eingaben (ID, Symbol, Name) und fragt Daten dynamisch per SQL-JOIN ab. Basis ist eine normalisierte PostgreSQL-Datenbank. Die Ergebnisse werden direkt formatiert im Terminal ausgegeben.
